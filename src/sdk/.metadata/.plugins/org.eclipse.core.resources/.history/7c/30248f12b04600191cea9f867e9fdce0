#include "peripheral.h"
#include "xgpio.h"
#include <stdio.h>

void timerCallback()
{
	xil_printf("timer tick!");
}

int main(void)
{
	timerSetup(&timerCallback);

	XGpio gpio;
	XGpio_Initialize(&gpio, 1);

	for(;;);
}
